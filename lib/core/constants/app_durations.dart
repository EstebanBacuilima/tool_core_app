/// Animation / timing constants.
class AppDurations {
  AppDurations._();

  /// Minimum time the splash stays visible, even if the session check
  /// resolves faster. Tune the milliseconds here.
  static const Duration splashMinDisplay = Duration(milliseconds: 1800);
}
